frontend:
  phases:
    build:
      commands:
        - export NODE_OPTIONS="--max_old_space_size=4096"
        - npm ci
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
